export type Nav = {
  href: string;
  text: string;
};

export type Blog = {
  title: string;
  slug: string;
  date: string;
  read: string;
  description: string;
  cover: string;
};

export type Project = {
  title: string;
  slug: string;
  date: string;
  type: string;
  description: string;
  link: string;
  cover: string;
  banner: string;
};

export type message = {
  status: number;
};
export type Snippet = {
  title: string;
  slug: string;
  description: string;
};
